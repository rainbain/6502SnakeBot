MEMORY {
    ZP:     start = $0000, size = $0100, type = rw;
    RAM:    start = $0100, size = $3F00, type = rw;
    ROM:    start = $8000, size = $7FEF, type = ro;
}

SEGMENTS {
    CODE: load = ROM, type = ro;
    DATA: load = RAM, type = rw;
    BSS:  load = RAM, type = bss;
}
